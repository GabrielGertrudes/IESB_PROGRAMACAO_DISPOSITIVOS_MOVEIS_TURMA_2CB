export const operacoes = ["soma", "subtração", "multiplicação", "divisão"];

export const soma = (a, b) => a + b;
export const subtracao = (a, b) => a - b;
export const multiplicacao = (a, b) => a * b;
export const divisao = (a, b) => a / b;
